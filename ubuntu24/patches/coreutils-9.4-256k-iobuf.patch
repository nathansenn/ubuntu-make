--- /tmp/u24clean2/coreutils-9.4/src/ioblksize.h	2023-08-29 11:39:27.000000000 +0000
+++ coreutils-9.4/src/ioblksize.h	2026-08-12 23:03:33.786633114 +0000
@@ -71,7 +71,7 @@
    In the future we could use the above method if available
    and default to io_blksize() if not.
  */
-enum { IO_BUFSIZE = 128 * 1024 };
+enum { IO_BUFSIZE = 256 * 1024 };
 static inline idx_t
 io_blksize (struct stat sb)
 {
--- /tmp/u24clean2/coreutils-9.4/src/wc.c	2023-08-29 11:39:27.000000000 +0000
+++ coreutils-9.4/src/wc.c	2026-08-12 23:03:33.794633181 +0000
@@ -51,7 +51,7 @@
   proper_name ("David MacKenzie")
 
 /* Size of atomic reads. */
-#define BUFFER_SIZE (16 * 1024)
+#define BUFFER_SIZE (256 * 1024)
 
 #ifdef USE_AVX2_WC_LINECOUNT
 /* From wc_avx2.c */
--- /tmp/u24clean2/coreutils-9.4/src/tr.c	2023-08-29 11:39:27.000000000 +0000
+++ coreutils-9.4/src/tr.c	2026-08-12 23:32:56.317244378 +0000
@@ -239,7 +239,7 @@
    It is set in main and used there and in validate().  */
 static bool translating;
 
-static char io_buf[BUFSIZ];
+static char io_buf[256 * 1024];
 
 static char const *const char_class_name[] =
 {
--- /tmp/u24clean2/coreutils-9.4/src/head.c	2023-08-27 16:33:32.000000000 +0000
+++ coreutils-9.4/src/head.c	2026-08-13 21:05:38.356145005 +0000
@@ -193,7 +193,7 @@
 static enum Copy_fd_status
 copy_fd (int src_fd, uintmax_t n_bytes)
 {
-  char buf[BUFSIZ];
+  static char buf[256 * 1024];
   const size_t buf_size = sizeof (buf);
 
   /* Copy the file contents.  */
@@ -253,7 +253,7 @@
   bool ok = true;
 
 #ifndef HEAD_TAIL_PIPE_READ_BUFSIZE
-# define HEAD_TAIL_PIPE_READ_BUFSIZE BUFSIZ
+# define HEAD_TAIL_PIPE_READ_BUFSIZE (256 * 1024)
 #endif
 #define READ_BUFSIZE HEAD_TAIL_PIPE_READ_BUFSIZE
 
@@ -500,7 +500,7 @@
 {
   struct linebuffer
   {
-    char buffer[BUFSIZ + 1];
+    char buffer[HEAD_TAIL_PIPE_READ_BUFSIZE + 1];
     size_t nbytes;
     size_t nlines;
     struct linebuffer *next;
@@ -522,7 +522,7 @@
      n_elide newlines, or until EOF, whichever comes first.  */
   while (true)
     {
-      n_read = safe_read (fd, tmp->buffer, BUFSIZ);
+      n_read = safe_read (fd, tmp->buffer, HEAD_TAIL_PIPE_READ_BUFSIZE);
       if (n_read == 0 || n_read == SAFE_READ_ERROR)
         break;
 
@@ -553,7 +553,7 @@
       /* If there is enough room in the last buffer read, just append the new
          one to it.  This is because when reading from a pipe, 'n_read' can
          often be very small.  */
-      if (tmp->nbytes + last->nbytes < BUFSIZ)
+      if (tmp->nbytes + last->nbytes < HEAD_TAIL_PIPE_READ_BUFSIZE)
         {
           memcpy (&last->buffer[last->nbytes], tmp->buffer, tmp->nbytes);
           last->nbytes += tmp->nbytes;
@@ -774,8 +774,8 @@
 static bool
 head_bytes (char const *filename, int fd, uintmax_t bytes_to_write)
 {
-  char buffer[BUFSIZ];
-  size_t bytes_to_read = BUFSIZ;
+  static char buffer[256 * 1024];
+  size_t bytes_to_read = sizeof buffer;
 
   while (bytes_to_write)
     {
@@ -799,11 +799,11 @@
 static bool
 head_lines (char const *filename, int fd, uintmax_t lines_to_write)
 {
-  char buffer[BUFSIZ];
+  static char buffer[256 * 1024];
 
   while (lines_to_write)
     {
-      size_t bytes_read = safe_read (fd, buffer, BUFSIZ);
+      size_t bytes_read = safe_read (fd, buffer, sizeof buffer);
       size_t bytes_to_write = 0;
 
       if (bytes_read == SAFE_READ_ERROR)
--- /tmp/u24clean2/coreutils-9.4/src/tail.c	2026-08-13 20:49:33.000000000 +0000
+++ coreutils-9.4/src/tail.c	2026-08-13 21:05:49.756167002 +0000
@@ -77,6 +77,11 @@
 /* Number of items to tail.  */
 #define DEFAULT_N_LINES 10
 
+/* Heap-backed pipe reads.  Stack loops stay on BUFSIZ.  */
+#ifndef HEAD_TAIL_PIPE_READ_BUFSIZE
+# define HEAD_TAIL_PIPE_READ_BUFSIZE (256 * 1024)
+#endif
+
 /* Special values for dump_remainder's N_BYTES parameter.  */
 #define COPY_TO_EOF UINTMAX_MAX
 #define COPY_A_BUFFER (UINTMAX_MAX - 1)
@@ -439,8 +444,8 @@
   n_written = 0;
   while (true)
     {
-      char buffer[BUFSIZ];
-      size_t n = MIN (n_remaining, BUFSIZ);
+      static char buffer[256 * 1024];
+      size_t n = MIN (n_remaining, sizeof buffer);
       size_t bytes_read = safe_read (fd, buffer, n);
       if (bytes_read == SAFE_READ_ERROR)
         {
@@ -629,7 +634,7 @@
 {
   struct linebuffer
   {
-    char buffer[BUFSIZ];
+    char buffer[HEAD_TAIL_PIPE_READ_BUFSIZE];
     size_t nbytes;
     size_t nlines;
     struct linebuffer *next;
@@ -648,7 +653,7 @@
   /* Input is always read into a fresh buffer.  */
   while (true)
     {
-      n_read = safe_read (fd, tmp->buffer, BUFSIZ);
+      n_read = safe_read (fd, tmp->buffer, HEAD_TAIL_PIPE_READ_BUFSIZE);
       if (n_read == 0 || n_read == SAFE_READ_ERROR)
         break;
       tmp->nbytes = n_read;
@@ -671,7 +676,7 @@
       /* If there is enough room in the last buffer read, just append the new
          one to it.  This is because when reading from a pipe, 'n_read' can
          often be very small.  */
-      if (tmp->nbytes + last->nbytes < BUFSIZ)
+      if (tmp->nbytes + last->nbytes < HEAD_TAIL_PIPE_READ_BUFSIZE)
         {
           memcpy (&last->buffer[last->nbytes], tmp->buffer, tmp->nbytes);
           last->nbytes += tmp->nbytes;
@@ -767,7 +772,7 @@
 {
   struct charbuffer
   {
-    char buffer[BUFSIZ];
+    char buffer[HEAD_TAIL_PIPE_READ_BUFSIZE];
     size_t nbytes;
     struct charbuffer *next;
   };
@@ -786,7 +791,7 @@
   /* Input is always read into a fresh buffer.  */
   while (true)
     {
-      n_read = safe_read (fd, tmp->buffer, BUFSIZ);
+      n_read = safe_read (fd, tmp->buffer, HEAD_TAIL_PIPE_READ_BUFSIZE);
       if (n_read == 0 || n_read == SAFE_READ_ERROR)
         break;
       *read_pos += n_read;
@@ -797,7 +802,7 @@
       /* If there is enough room in the last buffer read, just append the new
          one to it.  This is because when reading from a pipe, 'nbytes' can
          often be very small.  */
-      if (tmp->nbytes + last->nbytes < BUFSIZ)
+      if (tmp->nbytes + last->nbytes < HEAD_TAIL_PIPE_READ_BUFSIZE)
         {
           memcpy (&last->buffer[last->nbytes], tmp->buffer, tmp->nbytes);
           last->nbytes += tmp->nbytes;
@@ -866,11 +871,11 @@
 start_bytes (char const *pretty_filename, int fd, uintmax_t n_bytes,
              uintmax_t *read_pos)
 {
-  char buffer[BUFSIZ];
+  static char buffer[256 * 1024];
 
   while (0 < n_bytes)
     {
-      size_t bytes_read = safe_read (fd, buffer, BUFSIZ);
+      size_t bytes_read = safe_read (fd, buffer, sizeof buffer);
       if (bytes_read == 0)
         return -1;
       if (bytes_read == SAFE_READ_ERROR)
@@ -906,8 +911,8 @@
 
   while (true)
     {
-      char buffer[BUFSIZ];
-      size_t bytes_read = safe_read (fd, buffer, BUFSIZ);
+      static char buffer[256 * 1024];
+      size_t bytes_read = safe_read (fd, buffer, sizeof buffer);
       if (bytes_read == 0) /* EOF */
         return -1;
       if (bytes_read == SAFE_READ_ERROR) /* error */
--- /tmp/u24clean2/coreutils-9.4/src/tee.c	2023-07-05 11:09:55.000000000 +0000
+++ coreutils-9.4/src/tee.c	2026-08-13 20:45:25.364300586 +0000
@@ -227,7 +227,7 @@
   size_t n_outputs = 0;
   FILE **descriptors;
   bool *out_pollable IF_LINT ( = nullptr);
-  char buffer[BUFSIZ];
+  static char buffer[256 * 1024];
   ssize_t bytes_read = 0;
   int i;
   int first_out = 0;  /* idx of first non-null output in descriptors */
--- /tmp/u24clean2/coreutils-9.4/src/yes.c	2023-07-05 11:09:55.000000000 +0000
+++ coreutils-9.4/src/yes.c	2026-08-13 20:45:25.464300326 +0000
@@ -89,10 +89,11 @@
     }
   while (++operandp < operand_lim);
 
-  /* Improve performance by using a buffer size greater than BUFSIZ / 2.  */
-  if (bufalloc <= BUFSIZ / 2)
+  /* Improve performance by using a buffer size greater than half of
+     the sequential write plateau (256 KiB).  */
+  if (bufalloc <= (256 * 1024) / 2)
     {
-      bufalloc = BUFSIZ;
+      bufalloc = 256 * 1024;
       reuse_operand_strings = false;
     }
 
--- /tmp/u24clean2/coreutils-9.4/src/tac.c	2023-07-31 17:55:44.000000000 +0000
+++ coreutils-9.4/src/tac.c	2026-08-13 20:45:25.624299908 +0000
@@ -58,10 +58,10 @@
 
 
 /* The number of bytes per atomic read. */
-#define INITIAL_READSIZE 8192
+#define INITIAL_READSIZE (256 * 1024)
 
 /* The number of bytes per atomic write. */
-#define WRITESIZE 8192
+#define WRITESIZE (256 * 1024)
 
 /* The string that separates the records of the file. */
 static char const *separator;
